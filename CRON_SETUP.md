# Настройка внешнего Cron Job для напоминаний

## Обзор

Для автоматической отправки напоминаний за день до записи нужно настроить внешний сервис, который будет ежедневно вызывать endpoint `/api/cron/send-reminders`.

## Бесплатные варианты Cron сервисов

### Вариант 1: cron-job.org (Рекомендуется) ⭐

**Преимущества:**
- ✅ Полностью бесплатно
- ✅ Неограниченное количество задач
- ✅ Надежный сервис
- ✅ Простая настройка
- ✅ Мониторинг выполнения
- ✅ Email уведомления при ошибках

**Лимиты:**
- Минимальный интервал: 1 минута
- Максимум 1 запрос в секунду

### Вариант 2: EasyCron

**Преимущества:**
- ✅ Бесплатный тариф
- ✅ До 20 cron задач
- ✅ Простой интерфейс

**Лимиты:**
- Только 20 задач на бесплатном тарифе
- Минимальный интервал: 1 час

### Вариант 3: GitHub Actions

**Преимущества:**
- ✅ Бесплатно для публичных репозиториев
- ✅ 2000 минут в месяц для приватных
- ✅ Полный контроль

**Недостатки:**
- ⚠️ Более сложная настройка
- ⚠️ Требует GitHub репозиторий

---

## Инструкция: Настройка cron-job.org

### Шаг 1: Регистрация

1. Перейдите на [cron-job.org](https://cron-job.org)
2. Нажмите **"Sign up"** в правом верхнем углу
3. Заполните форму регистрации:
   - Email
   - Пароль
   - Подтвердите email

### Шаг 2: Создание Cron Job

1. После входа нажмите **"Create cronjob"**

2. Заполните форму:

   **Title (Название):**
   ```
   Напоминания о записях
   ```

   **URL:**
   ```
   https://ваш-домен.vercel.app/api/cron/send-reminders
   ```
   
   > ⚠️ Замените `ваш-домен.vercel.app` на ваш реальный домен Vercel

   **Schedule (Расписание):**
   - Выберите **"Every day"** (Каждый день)
   - Время: **10:00** (или любое удобное время)
   - Timezone: **Europe/Moscow** (или ваш часовой пояс)

   **Request method:**
   - Выберите **GET**

   **Enable this cronjob:**
   - ✅ Включите галочку

3. Нажмите **"Create cronjob"**

### Шаг 3: Проверка работы

1. В списке ваших cron jobs найдите созданную задачу
2. Нажмите **"Run now"** для тестового запуска
3. Проверьте:
   - Статус выполнения (должен быть зеленый)
   - HTTP код ответа (должен быть 200)
   - Проверьте Telegram - должны прийти напоминания (если есть записи на завтра)

### Шаг 4: Настройка уведомлений (опционально)

1. Перейдите в **Settings** → **Notifications**
2. Включите **"Email notifications on failures"**
3. Вы будете получать email, если cron job не сработает

---

## Инструкция: Настройка GitHub Actions

Если вы предпочитаете GitHub Actions, создайте файл в вашем репозитории:

### Создайте файл `.github/workflows/daily-reminders.yml`

```yaml
name: Отправка ежедневных напоминаний

on:
  schedule:
    # Запуск каждый день в 10:00 UTC (13:00 по Москве)
    - cron: '0 10 * * *'
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  send-reminders:
    runs-on: ubuntu-latest
    
    steps:
      - name: Вызов endpoint напоминаний
        run: |
          curl -X GET https://ваш-домен.vercel.app/api/cron/send-reminders
```

**Преимущества этого подхода:**
- Все в вашем репозитории
- Можно запускать вручную через GitHub UI
- Бесплатно

**Как запустить вручную:**
1. Перейдите в GitHub → ваш репозиторий → Actions
2. Выберите "Отправка ежедневных напоминаний"
3. Нажмите "Run workflow"

---

## Безопасность

### Защита endpoint от несанкционированного доступа (опционально)

Если вы хотите, чтобы только ваш cron сервис мог вызывать endpoint, добавьте секретный ключ:

1. **Обновите `.env`:**
   ```env
   CRON_SECRET=ваш_секретный_ключ_12345
   ```

2. **Обновите endpoint** `/app/api/cron/send-reminders/route.ts`:
   ```typescript
   export async function GET(request: NextRequest) {
     // Проверка секретного ключа
     const authHeader = request.headers.get('authorization');
     const expectedAuth = `Bearer ${process.env.CRON_SECRET}`;
     
     if (authHeader !== expectedAuth) {
       return NextResponse.json(
         { error: 'Unauthorized' },
         { status: 401 }
       );
     }
     
     // ... остальной код
   }
   ```

3. **Настройте заголовок в cron-job.org:**
   - В настройках cron job найдите **"Request headers"**
   - Добавьте:
     ```
     Authorization: Bearer ваш_секретный_ключ_12345
     ```

---

## Проверка работы системы

### Тестирование напоминаний:

1. **Создайте тестовую запись на завтра:**
   - Перейдите на `http://localhost:3000/booking`
   - Создайте запись на завтрашнюю дату
   - Укажите ваш Telegram username

2. **Запустите cron job вручную:**
   - В cron-job.org нажмите "Run now"
   - Или вызовите endpoint напрямую:
     ```bash
     curl https://ваш-домен.vercel.app/api/cron/send-reminders
     ```

3. **Проверьте результат:**
   - Вы должны получить напоминание в Telegram
   - В логах Vercel должна быть информация о выполнении

### Просмотр логов в Vercel:

1. Перейдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Logs**
4. Найдите запросы к `/api/cron/send-reminders`
5. Проверьте статус и вывод

---

## Рекомендуемое расписание

**Для салона красоты рекомендую:**

- **Время:** 10:00 утра по вашему часовому поясу
- **Причина:** Клиенты получат напоминание утром за день до визита
- **Альтернатива:** 18:00-19:00 вечера (напоминание вечером)

**Настройка в cron-job.org:**
```
Schedule: Every day at 10:00
Timezone: Europe/Moscow (GMT+3)
```

---

## Мониторинг

### Что отслеживать:

1. **Статус выполнения** в cron-job.org
2. **Логи Vercel** - проверяйте на ошибки
3. **Статистика из API** - количество отправленных напоминаний

### Пример ответа от API:

```json
{
  "success": true,
  "message": "Reminder notifications sent",
  "stats": {
    "total": 3,
    "successful": 3,
    "failed": 0
  }
}
```

---

## Часто задаваемые вопросы

**Q: Что если cron job не сработает?**
A: cron-job.org отправит вам email уведомление. Вы можете запустить его вручную.

**Q: Можно ли изменить время отправки?**
A: Да, просто измените расписание в настройках cron job.

**Q: Сколько это стоит?**
A: Полностью бесплатно при использовании cron-job.org или GitHub Actions.

**Q: Что если у меня нет записей на завтра?**
A: Endpoint вернет `total: 0`, это нормально. Ошибки не будет.

**Q: Можно ли отправлять напоминания за 2 дня?**
A: Да, нужно изменить логику в `/app/api/cron/send-reminders/route.ts`, изменив `tomorrow.setDate(tomorrow.getDate() + 1)` на `+ 2`.

---

## Итоговый чеклист

- [ ] Зарегистрироваться на cron-job.org
- [ ] Создать cron job с вашим Vercel URL
- [ ] Настроить расписание (10:00 ежедневно)
- [ ] Протестировать вручную через "Run now"
- [ ] Проверить получение напоминания в Telegram
- [ ] Включить email уведомления об ошибках
- [ ] Проверить логи в Vercel Dashboard

---

## Поддержка

Если возникнут проблемы:
1. Проверьте логи в Vercel Dashboard
2. Проверьте статус в cron-job.org
3. Убедитесь, что `TELEGRAM_BOT_TOKEN` настроен в переменных окружения Vercel
4. Проверьте, что есть записи на завтра с Telegram username

Готово! Ваша система напоминаний будет работать автоматически каждый день. 🎉
